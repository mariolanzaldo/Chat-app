apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: chat-service-pvc
  labels:
    app: chat-service-db
spec:
  #storageClassName: gp2
  resources:
    requests:
      storage: 512Mi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
